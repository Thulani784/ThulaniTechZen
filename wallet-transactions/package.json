{
  "name": "wallet-transactions",
  "version": "0.1.0",
  "description": "Minimal wallet-transactions API for CI/CD, security and traceability demo",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --runInBand",
    "lint": "eslint .",
    "build:package": "mkdir -p dist && tar -czf dist/wallet-transactions-${npm_package_version}.tgz src package.json"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "jest": "^29.0.0",
    "supertest": "^6.3.0"
  }
}
